import 'package:flutter/material.dart';

class FarmData extends StatelessWidget {
  const FarmData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('This is farm data'));
  }
}
